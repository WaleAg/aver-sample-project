# Example environment configuration for data_pipeline

# Database connection (defaults to SQLite in local folder)
DB_URL=sqlite:///transactions.db

# Input dataset (CSV file)
DATA_FILE=sample_data.csv
